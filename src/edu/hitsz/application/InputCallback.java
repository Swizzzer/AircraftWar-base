// This interface defines a callback for when input is received
package edu.hitsz.application;
public interface InputCallback {
    // This method is called when input is received
    void onInput(QuestionGUI questionBox);
}